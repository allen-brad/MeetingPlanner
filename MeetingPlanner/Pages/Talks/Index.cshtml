@page
@model MeetingPlanner.Pages.Talks.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].SpeakerFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].SpeakerLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].Topic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].Assigned)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].Accepted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Talk[0].Meeting)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Talk) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpeakerFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpeakerLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Assigned)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Accepted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meeting.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TalkID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TalkID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TalkID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
